//server layout for new user
import { NewVerificationForm } from "@/app/(no_nav_auth)/auth/(components)/new-verification-form";

const NewVerificationPage = () => {
  return <NewVerificationForm />;
};

export default NewVerificationPage;
