//server layout for reset password form
import { NewPasswordForm } from "@/app/(no_nav_auth_layout)/auth/(components)/new-password-form";

const NewPasswordPage = () => {
  return <NewPasswordForm />;
};

export default NewPasswordPage;
